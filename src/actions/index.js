export const navigate = (screen) => ({
  type: 'NAVIGATE',
  screen
})
export const pushScreen = (screen) => ({
  type: 'PUSH',
  screen
})
export const navigateTo = (screen) => ({
  type: 'NAVIGATE_TO',
  screen
})
